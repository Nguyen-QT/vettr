# Vettr — Bespoke Booking & Selection Engine - Project Guide

## 🛠️ Tech Stack & Tooling Core
- **Framework:** Next.js 15+ (App Router, Server Actions)
- **Language:** TypeScript (Strict Mode, explicit types mandatory, `any` forbidden)
- **Database & ORM:** PostgreSQL + Prisma ORM
- **Styling:** Tailwind CSS + Shadcn UI (High-end aesthetic execution)
- **State & Validation:** Zod (Schema validation), React Server Actions

## 🏗️ Architecture & Engineering Best Practices
This project strictly enforces **Domain-Driven Design (DDD)**, **Clean Architecture**, and **SOLID** principles within the Next.js framework:
- **Framework Isolation:** Next.js pages and API routes are "dumb" delivery mechanisms. Business logic lives inside pure domain services.
- **Domain-Driven Structure:** Code is grouped by business boundaries, not technical types.
- **Single Responsibility (SOLID):** UI components only render views. Business mutations live in Server Actions. Data orchestration lives in custom React hooks.
- **Data Encapsulation:** Enforce structural boundaries using the pattern: `Data Gateway (Prisma) -> Domain Service (Business Logic) -> Controller/Action -> View`.
- **Domain Boundary Isolation:** A domain's own Prisma models are queried and mutated only from within that domain's own `services/` directory. A service that needs another domain's data, or needs to mutate another domain's rows, must call that other domain's exported service function -- never reach through `prisma`/a transaction client directly into a table it doesn't own (e.g. `billing` must never query or write `BookingRequest`; `booking` must never query or write `TimeSlot`). Where the need is read-only, the owning domain should expose a narrow, purpose-built projection (smaller than its own primary summary type) rather than handing over its full row shape. Composing two domains' independent reads together (e.g. joining two lists for one view) belongs in the Controller/Action or View layer, not inside either domain's own service.
- **External Side-Effect Safety:** Any domain service that calls a third-party API with a real-world side effect (a payment charge, a refund, an email send, any external mutation that can't be locally rolled back) must satisfy all of the following -- surfaced by a code review catching `refundDeposit` (7.3.3) missing every one of them at first:
  1. **Never let the external call throw uncaught.** Wrap it in `try`/`catch` and return the domain's normal `{ success: false, error: ... }` shape, matching every other validation failure in that same function -- an unhandled throw from a third-party SDK must never propagate up to the Controller/Action or Route Handler layer as a raw exception.
  2. **Treat "the call failed" and "the call succeeded but our own follow-up write failed" as different outcomes, not the same catch block.** If the external call itself throws, nothing happened -- report failure. If the external call *succeeds* and a subsequent local write (e.g. persisting the resulting id) then throws, the side effect already happened in the real world regardless of whether that write lands -- reporting failure here is factually wrong and risks a caller retrying and re-triggering the same external effect (e.g. a second refund attempt against an already-refunded charge). Log the inconsistency for visibility and still report success.
  3. **Pair a synchronous call that has this failure mode with an asynchronous, independently-authoritative confirmation channel as a reconciliation backstop** -- for Stripe, that's a webhook event mirrored for both directions: `createDepositPaymentIntent` (fires the charge) is backstopped by `confirmDepositPayment`, invoked from the `payment_intent.succeeded` webhook; `refundDeposit` (fires the refund) is backstopped by `confirmDepositRefund`, invoked from the `refund.updated` webhook once `status === "succeeded"`. The webhook handler is idempotent and safe to re-run, and a non-2xx response from it makes Stripe automatically retry delivery -- so a transient failure in the synchronous write path self-heals within that retry window instead of leaving permanently inconsistent state. Any new synchronous call to an external side-effecting API needs this same synchronous-call-plus-async-confirmation pair, not just the synchronous half.
- **Mandatory File-Splitting (Single Responsibility):** Every domain that requires runtime input validation must decompose into three decoupled files at its root: `constants.ts` (domain arrays, baselines, and boundaries), `types.ts` (pure, standalone TypeScript entity contracts — never derived via Zod's `z.infer`), and `[domain].schema.ts` (Zod runtime validation only, importing from the other two). This is a mandatory technical step for every current and future domain (`booking`, `scheduling`, `billing`), not just booking.
- **Independent Mutation State Isolation:** A Domain Hook & Logic (`use*`) wrapping two or more independently-triggerable mutations must give each its own `isPending`/`error` state (its own `useTransition`, its own `useState<string | null>`), never one shared pair covering all of them -- surfaced by a review of `useCheckout` (7.5.6) sharing one `isPending`/`error` across `addAddon`/`removeAddon`/`finalize`. A shared pair lets an older action's result clobber a newer one's error after both resolve out of order, and can't tell the view which specific action (or, for a per-item action like removing one of several addons, which specific item) is actually in flight -- both real, not just cosmetic, since the view has no correct way to disable only the right control. Existing hooks built before this rule (e.g. `useArtistScheduleSettings`'s `setDayHours`/`setOverride` sharing one `isPending`) are a known gap, tracked in the Backlog rather than fixed retroactively by this rule alone.

### 🎨 Brand System & Semantic Styling Guardrails
- **Visual Aesthetic & Layout (Airbnb-Inspired):** Vettr's visual identity borrows Airbnb's layout language, driven entirely by Vettr's own custom palette (below) -- never Airbnb's actual brand colors.
  - **Typography & Scale:** Large, bold, highly readable headers; generous vertical rhythm between sections; clean sans-serif typography throughout.
  - **Component Structure:** Floating, high-contrast action cards (e.g. an Airbnb-style booking box), rounded pill badges for statuses/tags, subtle divider lines (`<Separator />`) between sections, and clean grid card layouts for listings (the artist directory, booking lists).
  - **Spacing & Density:** Spacious, breathable padding around forms, media, and detail rows -- avoid cramped, dense layouts.
  - **Micro-Details:** Subtle border outlines (`border-border`), soft rounded corners (`--radius` in the `0.5rem`-`0.75rem` range), and soft drop-shadows on floating/sticky elements (e.g. the dashboard header, a sticky action box).
- **Color Mapping (Custom Palette):** Vettr's palette is `#B775DA` (Vibrant Purple), `#C698DE` (Soft Lavender), `#EABEE3` (Soft Pink/Orchid), and `#ECBCC0` (Blush Rose) -- defined as CSS custom properties in `globals.css` (`:root`/`.dark`), never referenced by hex in components. Intended semantic roles:
  - `#B775DA` -- primary accent and primary CTAs: main action buttons, focus rings, active states (`bg-primary`/`ring`-family tokens).
  - `#C698DE` -- secondary highlights: hover states, selected toggles, subtle interactive elements (`bg-secondary`-family tokens).
  - `#EABEE3` -- card fills and muted surfaces, used at low opacity: badge backgrounds, selected-card highlights, light container fills (`bg-muted`/`bg-accent`-family tokens).
  - `#ECBCC0` -- soft statuses and secondary tags: subtle notification tags, soft warnings, secondary chips (a `chart-*`-family token reserved for tag/chip accents, not a primary status color).
- **Strict Semantic Token Rule:** Never hardcode explicit Tailwind color utilities (e.g. `bg-black`, `text-gray-900`, `border-slate-200`, `bg-blue-600`) or raw hex codes inline in components. ALL styling must strictly consume semantic CSS variables and Tailwind token classes (`bg-background`, `text-foreground`, `bg-card`, `text-muted-foreground`, `bg-primary`, `border-border`, `bg-accent`) -- the palette above only ever enters the codebase via `globals.css`'s token definitions.
- **Dark/Light Mode Native Support:** Components must dynamically adapt to dark and light modes via CSS root/dark tokens defined in `globals.css` without requiring custom state hooks or conditional inline classes.
- **Micro-Polish Deferral:** The layout/color guardrails above apply to every component going forward, not just in a future pass. What's still deferred to a dedicated design pass post-Phase 6 is specifically fine-grained micro-animations, glassmorphism overlays, and marketing polish -- motion and flourish, not the structural aesthetic or color system itself.

## 🧪 Automated Testing Guardrails
- **Mandatory Coverage:** Every core domain service (especially booking logic, slot allocations, and billing multipliers) must have accompanying unit tests.
- **Edge-Case Matrix:** Tests must explicitly cover race conditions, double-booking attempts, invalid timezone bounds, and past cancellation tracking.
- **The CLI Verification Rule:** Before marking any task in the roadmap as complete, Claude must execute `npm run test` in the terminal and verify a 100% green pass rate.
- **Manual E2E Verification Gate:** Playwright (`e2e/`, `playwright.config.ts`) is a second, explicitly *manual* testing layer on top of the automated one above. For any sub-task touching a view/route layer, Claude must add or update the relevant `e2e/*.spec.ts`, run `npm run test:e2e` headless as a sanity check, then point the user to `npm run test:e2e:ui` (Playwright UI Mode) to interactively click through the affected flow themselves before giving their go-ahead to commit. A headless `npm run test:e2e` pass does not substitute for the user's own UI Mode pass.
- **CI Pipeline (GitHub Actions, `.github/workflows/ci.yml`):** Runs on every PR against `main` and every push to `main` -- a real Postgres 16 service container, `prisma migrate deploy` replaying every migration from scratch, `next typegen` (ambient route types a fresh checkout never has, unlike a locally-run-dev environment), `tsc --noEmit`, `eslint .`, `npm run test`. Stripe/UploadThing env vars are non-secret placeholder strings, sufficient since the unit suite mocks Stripe at the module boundary per-test rather than making a real external call. Deliberately first-tier only (typecheck + lint + unit tests against a real but ephemeral DB) -- Stripe test-mode integration tests and e2e-in-CI are staged follow-ups, captured in the Backlog below rather than built here. `eslint.config.mjs` downgrades `react-hooks/set-state-in-effect` to `warn`: eslint-plugin-react-hooks v7 (bundled with Next 16.3.5's `eslint-config-next`) ships it as a hard error and flags the ordinary fetch-on-mount/sync-to-external-source effect pattern used throughout the app, not just genuine cascading-render bugs -- surfaced when this pipeline's lint step ran the existing codebase through it for the first time, on files unrelated to the pipeline itself. The sites it actually flagged are captured in the Backlog below rather than fixed as a side effect of adding CI.

## 🧭 Project Blueprint & Core Constraints
This app is *not* a standard calendar app (like Calendly). It is a **curation and screening tool**. 
1. **No Auto-Booking:** Clients request slots (11:00 AM, 2:00 PM, 5:30 PM) as start-time candidates only — they never set the service duration. The artist decides duration upon reviewing the request, and no database allocation occurs until the artist manually clicks "Approve". If the artist-decided duration overflows the requested slot's length, the booking consumes the next adjacent slot too and marks it unavailable (for now, capped at two consecutive slots — typically only FREESTYLE services need this).
2. **Visual Enforcement & Filtering:** Every request *must* contain an Instagram handle and at least one high-resolution design reference image. If the text input contains blacklisted terms for basic/simple work (and no advanced modifiers), programmatically reject it with a friendly redirection message before database persistence occurs.
3. **Flexible Pricing & Final Bill:** The schema must support a structural `depositPaid` flag, a dynamic base `estimatedPrice`, and line-item `addons` added dynamically on the day of service.
4. **Flagging Mechanism:** The `ClientProfile` model must track cancellation history and automatically toggle an `enforcePrecharge` state (50% upfront penalty) if flagged.

## 📁 Repository Directory Blueprint
```text
src/
├── app/                      # Next.js Routing Layer (Dumb Delivery)
│   ├── layout.tsx
│   ├── page.tsx              # Landing / Root routing
│   ├── artist/               # Artist Dashboard Routing
│   └── book/                 # Client Booking Form Routing
├── components/               # Cross-cutting UI Components (Shadcn, primitives)
├── domains/                  # CORE DOMAIN LAYER (DDD & Clean Architecture)
│   ├── booking/               # Domain 1: Client briefs, image vetting, handle tracking
│   │   ├── components/       # Domain-specific UI (VisualBookingForm, RequestCard)
│   │   ├── hooks/            # Presentation state orchestrators (useBookingApproval)
│   │   ├── services/         # Pure business logic (validateComplexity, routeRequest)
│   │   ├── constants.ts      # Domain arrays, baselines, and boundaries
│   │   ├── types.ts          # Pure TypeScript entity contracts (no z.infer)
│   │   ├── booking.schema.ts # Zod runtime validation only
│   │   └── actions.ts        # Next.js Server Actions boundary
│   ├── scheduling/           # Domain 2: Slot booking, flex-shifts, concurrent blocks
│   │                         # (same constants.ts / types.ts / scheduling.schema.ts split)
│   └── billing/              # Domain 3: Dynamic tabs, cancellation penalty engines
│                              # (same constants.ts / types.ts / billing.schema.ts split)
├── lib/                      # Framework/Utility infrastructure configuration
│   ├── prisma.ts             # Prisma Client instance singleton
│   └── utils.ts              # Tailwind merging utilities
└── types/                    # System-wide global enterprise type declarations
```

## ⚡ Common Development Commands
- **Install Dependencies:** `npm install`
- **Run Local Dev Server:** `npm run dev`
- **Prisma Studio Visualizer:** `npx prisma studio`
- **Generate Client:** `npx prisma generate`
- **Database Migration:** `npx prisma migrate dev`
- **Execute Test Suite:** `npm run test`

## 🗺️ Current Status & Roadmap
> **Structural mandate (applies to every phase below):** any task that introduces domain input validation must follow the `constants.ts` / `types.ts` / `[domain].schema.ts` split defined in the Architecture section — this is not intake-specific and carries forward into Phase 2's remaining tasks and into the `scheduling`/`billing` domains built in Phases 3-4.

### 📦 Phase 1: Database Foundation & Domain Mapping
- [x] 1.1: Initialize PostgreSQL schema via Prisma.

- [x] 1.2: Setup core entity relationships (`Artist`, `ClientProfile`, `IntakeRequest`, `TimeSlot`).

### 📦 Phase 2: Core Intake Engine & Visual Guardrails
- [x] **2.1: Client Input Validation Layers** (Build Zod schemas for validation, including strict Instagram handle regex and image string array boundaries).

- [x] **2.2: UploadThing Backend Integration** (Configure the UploadThing backend API route handlers and initial server endpoint configurations).

- [x] **2.3: Pure Business Logic Core** (Write `services/validateComplexity.ts` to inspect incoming fields, flag empty sets, and hook up unit tests).

- [x] **2.4: React Server Action Layer** (Build `actions.ts` to intake user submissions, pipe to domain services, and save a PENDING record in Prisma).

- [x] **2.5: Visual Intake Form Component** (Construct the frontend `VisualIntakeForm.tsx` using Shadcn primitives, linking directly to the server action), decomposed per the Mandatory Task Breakdown Rule:
  - [x] 2.5.1: UI Primitive & Config Setup (Shadcn init/primitives, UploadThing client helper, image config).
  - [x] 2.5.2: Domain Hook & Logic (`useVisualIntakeForm` state/validation orchestration).
  - [x] 2.5.3: View & Route (`VisualIntakeForm.tsx` composition + `/book/[artistId]` routing).

### 📦 Phase 3: Artist Decision Dashboard
- [x] **3.1: Dashboard Layout Scaffold** (Build basic layout wrapper with authentic mobile-first rendering tailored for working artists).

- [x] **3.2: Interactive Review Cards** (Build component that accepts intake data and renders 1-click Instagram deep links and reference images).

- [x] **3.3: Action Mutators (Approve/Decline)** (Write server actions to toggle intake request enums and generate automatic response message copies).

### 📦 Phase 4: Concurrency Rules & Financial Logic
- [x] **4.1: Reservation Lock-In Timers & Slot Confirmation UI** (Design database state checks to ensure a time slot isn't allocated to two approved clients concurrently, then wire that confirmation into the artist dashboard. Service duration is an artist decision made at approval time, not a client input; a duration that overflows the requested slot consumes and locks the next adjacent slot too, capped at two consecutive slots for now), decomposed per the Mandatory Task Breakdown Rule:
  - [x] **4.1.1: Scheduling Domain Scaffold** (`constants.ts`/`types.ts`/`scheduling.schema.ts` + the `TimeSlot` overlap-exclusion constraint).
  
  - [x] **4.1.2: `confirmTimeSlot` Domain Service** (concurrency-safe slot allocation, with unit tests covering race conditions and double-booking).

  - [x] **4.1.3: Server Action Boundary** (`confirmTimeSlotAction`).

  - [x] **4.1.4: Data Gateway** (`IntakeRequest.requestedStartTime`, `IntakeRequest.proposedDurationMinutes`, and a new `RequestStatus.AWAITING_SLOT_CONFIRMATION` value).
  
  - [x] **4.1.5: Intake Capture** (client picks a date + one of the artist's fixed daily times (11:00/14:00/17:30) on the intake form; saved as `requestedStartTime`).
  
  - [x] **4.1.6: Review/Propose Domain Service** (artist enters a duration; a single-slot fit atomically books + approves, a two-slot spillover stores the proposal and moves to `AWAITING_SLOT_CONFIRMATION` instead of booking, since that case needs off-platform confirmation with the client first).
  
  - [x] **4.1.7: Confirm-Proposed-Booking Domain Service** (finalizes an `AWAITING_SLOT_CONFIRMATION` request once the artist has confirmed the double-slot booking with the client off-platform).
  
  - [x] **4.1.8: Controller/Action Boundary** (server actions for the review step and the confirm-booking step).
  
  - [x] **4.1.9: Artist Dashboard UI** (duration input on approve, the two outcome messages, and a "Confirm Booking" control for awaiting-confirmation requests).
  
  - [x] **4.1.10: Live Slot Availability** (client's date/time picker reflects real availability instead of any date being pickable -- deliberately sequenced after 4.1.6-4.1.9 so the core approve/confirm mechanics ship first), decomposed per the Mandatory Task Breakdown Rule:
    - [x] **4.1.10.1: Domain Service** (`getAvailableSlots(artistId, date)` in scheduling -- a fixed daily time is available if no `BOOKED` `TimeSlot` overlaps its `[time, time + MAX_SLOT_DURATION_MINUTES)` nominal window, with unit tests).
    - [x] **4.1.10.2: Controller/Action Boundary** (`getAvailableSlotsAction`).
    - [x] **4.1.10.3: Domain Hook & Logic** (`useVisualIntakeForm` fetches availability whenever the picked date changes).
    - [x] **4.1.10.4: View & Route** (disable already-booked time options in the intake form; e2e spec update).

- [x] **4.2: Mandatory Email Capture & Validation** (Enforce required email address input in client intake schemas, forms, and backend handlers to ensure deliverability of booking confirmation and magic link tokens), decomposed per the Mandatory Task Breakdown Rule:
  - [x] 4.2.1: Data Gateway & Domain Service (`ClientProfile.email` migrated to required; `intake.schema.ts`/`types.ts` tightened to match -- bundled into one PR since the Prisma-generated and Zod-inferred types must tighten together for `actions.ts` to type-check).
  - [x] 4.2.2: View & Route (`VisualIntakeForm.tsx` drops the "(optional)" label and marks email required; e2e spec update).

- [x] **4.3: Artist Business Hours & Operating Schedule Engine** (Define recurring weekly working hours, blackout days, and custom date overrides so the client slot picker only renders valid operating windows. No config for a given day defaults to fully open, matching today's behavior, until the artist explicitly restricts it), decomposed per the Mandatory Task Breakdown Rule:
  - [x] 4.3.1: Data Gateway (`ArtistWeeklyHours` and `ArtistScheduleOverride` Prisma models + migration).
  - [x] 4.3.2: Domain Service -- Read (`getOperatingWindows(artistId, date)` in scheduling: override-first, then weekly-hours-row, else default-open, with unit tests).
  - [x] 4.3.3: Domain Service -- Read/Compose (`getAvailableSlots` updated to intersect operating windows with the existing booked-slot check, with unit tests).
  - [x] 4.3.4: Domain Service -- Write (`setWeeklyHours`/`setScheduleOverride` upsert services with validation, with unit tests).
  - [x] 4.3.5: Controller/Action Boundary (server actions wrapping the read/write services for the settings UI).
  - [x] 4.3.6: Domain Hook & Logic (`useArtistScheduleSettings` orchestrating fetch/mutate state).
  - [x] 4.3.7: View & Route (artist-facing "Business Hours" settings section on the dashboard -- weekly hours grid + blackout/override date management; e2e spec).

- [x] **4.4: Mandatory Estimated Price Validation on Review** (Enforce that artists input an explicit estimatedPrice alongside duration when reviewing/approving an intake request. No new migration needed -- estimatedPrice already exists on IntakeRequest as a nullable Decimal; enforcement is at the application layer since rows stay null until reviewed), decomposed per the Mandatory Task Breakdown Rule but shipped as a single bundled PR (unlike 4.2's Data Gateway/Domain Service split, this coupling is a functional regression, not just a type error: making estimatedPrice required in reviewIntakeRequestAction before the UI supplies it would break the live Approve flow on main between merges, confirmed by the e2e suite). Also defaults the estimated price input to TIER_2's baseline minimum for FREESTYLE requests, since a client's own budget guess for freestyle work is often an overestimate:
  - [x] 4.4.1: Domain Service + required Controller/Action passthrough (`reviewIntakeRequestInputSchema` gains `estimatedPrice` validation; `reviewIntakeRequest.ts` persists it on both the immediate-approve and awaiting-confirmation paths; unit tests).
  - [x] 4.4.2: Domain Hook & Logic (`useRequestActions.review()` signature extended to accept `estimatedPrice`).
  - [x] 4.4.3: View & Route (`RequestActions.tsx` adds an estimated-price input alongside duration; e2e spec update).

- [x] **4.5: Artist Upcoming Appointments Dashboard** (A new read-only view of `APPROVED` requests with a future-dated `TimeSlot`, so an approved request doesn't just vanish from the artist's view on the next reload -- it moves to an upcoming-appointments view instead. No new schema needed), decomposed per the Mandatory Task Breakdown Rule:
  - [x] 4.5.1: Domain Service (`getUpcomingAppointments(artistId)` in intake -- `APPROVED` requests with a future-dated `TimeSlot`, joined with client/design references/estimatedPrice; unit tests).
  - [x] 4.5.2: View & Route (new `/artist/[artistId]/appointments` page + a read-only `AppointmentCard`; nav link; e2e spec).

- [x] **4.6: Tiered Reference Gallery** (Show clients example reference images grouped by COMPLEXITY_TIERS to help them choose a tier while filling out the intake form. Per-artist -- clients are judging that specific artist's style/pricing, not a generic gallery. Read-only for clients; artist upload/management UI is a backlog item), decomposed per the Mandatory Task Breakdown Rule:
  - [x] 4.6.1: Data Gateway (`TierReferenceImage` Prisma model: `artistId`, `tier`, `imageUrl` + migration).
  - [x] 4.6.2: Domain Service (`getTierReferenceImages(artistId)` in intake, grouped by tier; unit tests).
  - [x] 4.6.3: View & Route (fetched server-side in `/book/[artistId]/page.tsx`, passed to `VisualIntakeForm`, showing example images for whichever tier is currently selected; e2e spec).

### 📦 Phase 5: Authentication & Access Control
> Both Artist and Client are real accounts that log in -- not just a client-facing feature. The artist dashboard has had **no access control at all** since Phase 3: any URL with a guessable/known `artistId` currently works. Hand-rolled (password hashing + DB-backed sessions + middleware), not a library like Auth.js/NextAuth -- Auth.js's Credentials provider isn't meant for real password auth (the maintainers point OAuth/magic-link users at it instead), and its provider/adapter/callback model doesn't map onto this project's `constants.ts`/`types.ts`/`[domain].schema.ts` + domain-service pattern the way a plain domain service does. A shared `Account` (`email`, `passwordHash`, `role: ARTIST | CLIENT`, linked to exactly one of `Artist`/`ClientProfile`) + `Session` model covers both roles with one auth codepath.
- [x] **5.1: Artist Authentication & Route Protection** (Password login for the artist, and middleware protecting `/artist/[artistId]/*` so a session must be an ARTIST account whose linked `artistId` matches the URL. Artist accounts stay manually provisioned for now -- no signup UI; there's a single artist and Account rows are created the same way Artist rows already are, via Prisma Studio/a script), decomposed per the Mandatory Task Breakdown Rule:
  - [x] 5.1.1: Data Gateway (`Account`, `Session`, `AccountRole` enum + migration).
  - [x] 5.1.2: Domain Service (password hashing/verification, session create/lookup/delete, `loginArtist(email, password)`; unit tests).
  - [x] 5.1.3: Controller/Action (`loginAction`/`logoutAction`; sets/clears the session cookie).
  - [x] 5.1.4: Route Protection Middleware (shipped as Next 16's `src/proxy.ts` -- the framework's rename of `middleware.ts`, always Node.js runtime -- guarding `/artist/[artistId]/*`; redirects unauthenticated or artistId-mismatched requests to login. Bundled into one PR with 5.1.5, since the proxy alone would 404 every dashboard route until the login page existed).
  - [x] 5.1.5: View & Route (artist login page; logout control in the dashboard layout; e2e spec).

- [x] **5.2: Client Accounts & Booking Linkage** (Self-service client signup/login, completely decoupled from booking submission -- `submitIntakeRequest` stays guest-capable exactly as it works today. Password-based, same as artist auth (5.1), not magic link -- an eventual in-app chat between client and artist around booking review (backlog) needs frequent, low-friction re-entry that an email round-trip works against; a "new message" notification can still carry a scoped link later as a convenience layered on top of password auth, not a substitute for it. `signupClient` only *links* an `Account` to an existing `ClientProfile` found by email -- it never creates one, since an account with no booking history has nothing yet to manage; signup fails with a friendly message telling them to book first if no match exists, or that an account already exists if the match is already linked. Client-facing routes are session-derived (`/client`, not `/client/[clientProfileId]`) rather than keyed by a URL param. Known limitation, deferred rather than solved now: a guest booking made under a different Instagram handle than the one later used to sign up won't auto-link, and neither will one made under a different email than the one on file, since matching is keyed on a single field at a time (`instagramHandle` at intake, `email` at signup) and `ClientProfile.email` itself isn't unique), decomposed per the Mandatory Task Breakdown Rule:
  - [x] 5.2.1: Domain Service (`signupClient`/`loginClient` in `auth`, reusing `hashPassword`/`verifyPassword`/`createSession` from 5.1; `getClientBookings(clientProfileId)` in `intake`, spanning every artist a client has booked with; unit tests).
  - [x] 5.2.2: Controller/Action (`signupClientAction`/`loginClientAction`; generalizes `logoutAction` to redirect to `/artist/login` or `/client/login` based on the session's own role instead of the hardcoded artist path it has today. `getClientBookingsAction` dropped from scope -- the client dashboard (5.2.4) calls `getClientBookings` directly as a server component, matching the `getUpcomingAppointments`/`getTierReferenceImages` precedent).
  - [x] 5.2.3: Route Protection (extends `src/proxy.ts` to also guard `/client/*`, requiring a valid, unexpired session with role CLIENT -- no URL param to match against, unlike the artist check, since client routes are session-derived).
  - [x] 5.2.4: View & Route (client signup page, client login page, client dashboard listing bookings across all artists, logout control; e2e spec).

- [x] **5.3: Landing Page & Artist Discovery Directory** (Root `/` becomes a real landing page for a signed-out visitor, offering three explicit paths -- "I want to book an artist" (→ an artist directory page, then that artist's existing `/book/[artistId]` hub), "I have an existing or past booking" (→ `/client/login`), "I'm an artist" (→ `/artist/login`). A signed-in visitor hitting `/` skips the choice entirely and is redirected straight to `/client` or `/artist/[artistId]`. Preserves direct `/book/[artistId]` links (e.g. an artist's Instagram bio link) bypassing the directory entirely -- it's a fallback for organic traffic landing on the bare domain, not the only way in), decomposed per the Mandatory Task Breakdown Rule:
  - [x] 5.3.1: Data Gateway (`Artist` gains `avatarUrl`/`bio`/`location`, all nullable + migration -- read-only for clients, artist-facing management is a backlog item, seeded via Prisma Studio for now, same as `TierReferenceImage`).
  - [x] 5.3.2: Domain Service (`getArtistDirectory()` in a new `directory` domain -- a distinct bounded context from `intake`/`scheduling`/`auth`, this is public artist-browsing, not booking/vetting logic; unit tests).
  - [x] 5.3.3: View & Route (root `/` becomes the three-option landing page, auto-redirecting an already-authenticated visitor via `getCurrentSession()`; new `/artists` directory page + `ArtistDirectoryCard`, linking into the existing `/book/[artistId]`; e2e spec).

- [x] **5.4: Self-Service Booking Modification & Cancellation** (Allow clients to update pending request details or cancel pending/confirmed requests within policy windows via the Client Dashboard. Uses the existing plain `CANCELLED` status -- the richer `CANCELLED_BY_CLIENT`/`CANCELLED_BY_ARTIST`/`NO_SHOW` lifecycle and `ClientProfile` cancellation-offense counting stay scoped to 5.6, not duplicated here, so this needs no schema change. Editable fields on a PENDING request are notes, budget range, and requested date/time only -- not tier/tags/images, which would need rerunning the full complexity/validation pipeline for comparatively low value in a first pass. An APPROVED (already-booked) request can only be self-cancelled outside a 48-hour window before the appointment; PENDING/AWAITING_SLOT_CONFIRMATION have no locked slot yet so cancel freely. AWAITING_SLOT_CONFIRMATION/APPROVED are cancel-only, not editable -- both are already mid-negotiation or locked), decomposed per the Mandatory Task Breakdown Rule:
  - [x] 5.4.1: Domain Service (`cancelIntakeRequest`/`updatePendingIntakeRequest` in `intake`, both ownership-checked against the trusted session's `clientProfileId`; cancelling an `APPROVED` request releases its `BOOKED` `TimeSlot` rows to `RELEASED`; unit tests covering ownership mismatches, status guards, the window boundary, and slot-availability conflicts).
  - [x] 5.4.2: Controller/Action (`cancelIntakeRequestAction`/`updatePendingIntakeRequestAction` in `intake/actions.ts`, deriving `clientProfileId` from `getCurrentSession()` rather than trusting client-supplied input).
  - [x] 5.4.3: Domain Hook & Logic (`useClientBookingActions` orchestrating cancel/edit form state and refreshing the dashboard list).
  - [x] 5.4.4: View & Route (Cancel button and an inline edit form on the client dashboard's booking cards; e2e spec).

- [x] **5.5: Rescheduling & Slot Shift Engine** (Artist-initiated reschedule of an already-`APPROVED` booking -- the artist has final say over slot allocation, same "No Auto-Booking" model as approval itself, so a reschedule applies immediately rather than needing the client's on-platform confirmation. Client-initiated reschedule proposals, which would need a real propose/confirm handshake and notifications, are a separate backlog item. `PENDING`/`AWAITING_SLOT_CONFIRMATION` requests already have their time adjustable via 5.4's `updatePendingIntakeRequest` or the artist's own review step -- "rescheduling" only means something once a real `TimeSlot` is locked), decomposed per the Mandatory Task Breakdown Rule:
  - [x] 5.5.1: Domain Service (`rescheduleApprovedBooking` in `intake`, reusing scheduling's `createBookedTimeSlots`/`isSlotConflict`; releases the old `BOOKED` `TimeSlot`(s) and books the new one(s) in one transaction, updating `requestedStartTime`; unit tests covering conflicts, non-APPROVED rejection, and single vs. two-slot durations).
  - [x] 5.5.2: Controller/Action (`rescheduleApprovedBookingAction` in `intake/actions.ts`; new action, so it gets an ownership check from day one -- derives the session's `artistId` via `getCurrentSession()` and verifies it matches the request's `artistId`, same pattern as 5.4.2's client-side checks).
  - [x] 5.5.3: Domain Hook & Logic (`useRescheduleBooking` orchestrating the reschedule form state on the artist's upcoming-appointments view).
  - [x] 5.5.4: View & Route (a "Reschedule" control on `AppointmentCard` opening an inline date/time/duration form; e2e spec. Also fixed a real bug in `getUpcomingAppointments` surfaced by e2e testing -- it was reading/matching on ALL `TimeSlot` rows regardless of status, so a rescheduled request's leftover `RELEASED` slot got merged with its new `BOOKED` slot into a bogus mixed time range; fixed to filter to `status: "BOOKED"` in both the query and the include, with a regression test).

- [x] **5.6: Cancellation & No-Show Lifecycle Management** (Implement status transitions for COMPLETED, CANCELLED_BY_CLIENT, CANCELLED_BY_ARTIST, and NO_SHOW, updating ClientProfile cancellation offenses. Only an APPROVED-booking client cancellation or a no-show counts as a cancellation strike -- a PENDING/AWAITING cancellation has no locked slot yet, and an artist-initiated cancellation is never the client's fault. `STRIKE_THRESHOLD = 1`: one strike auto-toggles `enforcePrecharge`), decomposed per the Mandatory Task Breakdown Rule:
  - [x] 5.6.1: Data Gateway (`RequestStatus` migration: rename `CANCELLED` -> `CANCELLED_BY_CLIENT`, add `CANCELLED_BY_ARTIST` and `NO_SHOW`, via `ALTER TYPE ... RENAME VALUE` so existing rows carry their history forward. Bundled the minimal type-compat fixes needed to keep tsc/tests green, same coupling as 4.2.1).
  - [x] 5.6.2: Domain Service (`STRIKE_THRESHOLD` constant + shared `applyCancellationStrike` transaction helper; `cancelIntakeRequest` (5.4) updated to apply a strike only for an APPROVED cancellation; new `cancelApprovedBookingAsArtist` (artist-side cancel, never strikes), `markAppointmentNoShow` (past-due APPROVED only, strikes), `markAppointmentCompleted` (past-due APPROVED only, no strike); new `getPastDueAppointments` read -- APPROVED requests where every booked slot has already started, kept mutually exclusive with `getUpcomingAppointments`; unit tests).
  - [x] 5.6.3: Controller/Action (`cancelApprovedBookingAsArtistAction`, `markAppointmentNoShowAction`, `markAppointmentCompletedAction` in `intake/actions.ts`, ownership-checked via session `artistId`, same pattern as 5.5.2).
  - [x] 5.6.4: Domain Hook & Logic (`useAppointmentLifecycleActions` orchestrating Cancel/No-Show/Complete button state, alongside the existing `useRescheduleBooking`).
  - [x] 5.6.5: View & Route (a "Needs Resolution" section on the artist's appointments page, using `getPastDueAppointments`, with Mark Completed/Mark No-Show controls; e2e spec. Design correction made during this sub-task: Cancel lives on the *upcoming* list instead -- alongside Reschedule on `AppointmentActions` -- since "cancel" only makes sense for a booking that hasn't happened yet; a past-due appointment's only two real outcomes are completed or no-show, so `AppointmentLifecycleActions` doesn't offer it).

### 📦 Phase 6: Critical Access & Onboarding Fixes
> Sequenced ahead of the Financial Engine below: both items are live gaps in today's app (not speculative polish), and the Financial Engine likely wants real client names/ages on file before it starts charging anyone. Mandatory email capture was already covered by 4.2 -- no further work needed there.
- [x] **6.1: Logged-in Booking & Search Access** (Root `/`'s auto-redirect for a signed-in visitor -- straight to `/client` or `/artist/[artistId]` per 5.3's original design -- currently leaves a signed-in client with no way back to `/artists` to book with a different or additional artist. `/artists` and `/book/[artistId]` were never actually route-protected -- `src/proxy.ts`'s matcher only covers `/artist/:path*`/`/client/:path*` -- so the real gaps are UX-only: no visible link to get there from the client dashboard, and the intake form not knowing a signed-in client's existing contact details), decomposed per the Mandatory Task Breakdown Rule:
  - [x] 6.1.1: Domain Service (`getClientProfileContactDetails(clientProfileId)` in `intake` -- `{ instagramHandle, email, phone } | null`, a simple read for the form to prefill from; unit tests. No Controller/Action layer -- called directly from a server component, same precedent as `getClientBookings`/`getUpcomingAppointments`; no Data Gateway -- no schema change).
  - [x] 6.1.2: Domain Hook & Logic (`useVisualIntakeForm` accepts an optional `initialClientDetails`, used for the form's default values instead of empty strings).
  - [x] 6.1.3: View & Route (a "Find an artist" link on the client dashboard to `/artists`; `/book/[artistId]/page.tsx` looks up a signed-in client's session and passes their contact details into `VisualIntakeForm`, which locks -- disables/greys out -- the prefilled fields; e2e spec. Correctness fix bundled in: `submitIntakeRequest` now attaches a signed-in client's booking directly to `session.clientProfileId` and ignores submitted contact-field values entirely, rather than matching/upserting purely by `instagramHandle` -- an edited or client-side re-enabled field could previously attach the booking to a different profile than the one the client is logged into).

- [x] **6.2: Client Onboarding Required Fields** (`ClientProfile` currently only captures `instagramHandle`/`email`/`phone` -- add required `firstName`/`lastName`/`dateOfBirth` fields (a birth date rather than a raw age, so exact current age is always derivable rather than a stored value quietly going stale) across the schema, intake form, and validation schemas. Enforces a minimum age of 18. Bundles a correction to 6.1.3: that sub-task locked `instagramHandle`/`email`/`phone` as one shared all-or-nothing block whenever a signed-in client had any known details -- fine for those three since they're always populated once a `ClientProfile` exists, but these new required fields will be empty for every client who signed up before 6.2, so locking them the same way would permanently block existing clients from ever booking again. Switches to locking each field independently instead), decomposed per the Mandatory Task Breakdown Rule:
  - [x] 6.2.1: Data Gateway (`ClientProfile` gains nullable `firstName`/`lastName`/`dateOfBirth` (`@db.Date`) + migration -- nullable at the DB level, matching the established `requestedStartTime`/`estimatedPrice` precedent (CLAUDE.md 4.1e/4.4): existing rows simply keep null until a client provides them, with "required" enforced by `intake.schema.ts` for every new submission rather than a backfilled placeholder value).
  - [x] 6.2.2-6.2.4: Domain Service + Domain Hook & Logic + View & Route, shipped as a single bundled PR (unlike 6.2.1's clean split, confirmed by the e2e suite: making `firstName`/`lastName`/`dateOfBirth` required in `clientIntakeInputSchema` before the form actually collects them breaks every live guest submission on `main` between merges -- same functional-regression reasoning as 4.4, not just a type error):
    - Domain Service: extend `ClientIntakeInput`/`clientIntakeInputSchema`/`ClientProfileContactDetails` (6.1.1) with required `firstName`/`lastName`/`dateOfBirth`, enforcing the 18+ minimum via Zod; update `submitIntakeRequest`'s guest-upsert to include the new fields, and its signed-in path to fill in only currently-null fields on the existing `ClientProfile` rather than trusting/overwriting from submitted data, same ownership posture as 6.1.3; unit tests.
    - Domain Hook & Logic: `useVisualIntakeForm` gains the three new fields with defaults from `initialClientDetails`; switches `VisualIntakeForm`'s field-locking from one shared boolean to per-field, correcting 6.1.3's all-or-nothing lock (that sub-task locked `instagramHandle`/`email`/`phone` as one block whenever a signed-in client had any known details -- fine for those three since they're always populated once a `ClientProfile` exists, but these new required fields are empty for every client who signed up before 6.2, so locking them the same way would permanently block existing clients from ever booking again).
    - View & Route: add firstName/lastName/dateOfBirth inputs to `VisualIntakeForm.tsx`, each independently disabled when already known; e2e spec covering a guest submission requiring all three and rejecting under-18, a signed-in client with existing name/DOB seeing them locked, and a signed-in client with a null name/DOB still able to fill it in and submit.

- [x] **6.3: Client Max End Time** (Supersedes the backlog's former "Optional Client Max End Time" idea. Client-facing only, scoped down from the original artist-side-cap-plus-client-preference pairing: an artist who needs to restrict duration on their end already has business hours (4.3) for that. Purely advisory -- an optional per-booking "must be finished by" time the client flags on intake, for the artist to weigh when deciding duration/slot count during review. Never auto-enforced or blocking, matching "No Auto-Booking" -- the artist always has final say), decomposed per the Mandatory Task Breakdown Rule:
  - [x] 6.3.1: Data Gateway (`IntakeRequest.clientMaxEndTime` nullable `DateTime?` + migration).
  - [x] 6.3.2: Domain Service (`clientIntakeInputSchema`/`ClientIntakeInput` gain an optional client end-time field, combined with `requestedDate` the same way `combineRequestedDateAndTime` works; `submitIntakeRequest` persists it; `getPendingIntakeRequests`/`PendingIntakeRequestSummary` expose it for the artist's review. No new Controller/Action layer -- existing actions just gain the field. Unit tests).
  - [x] 6.3.3: Domain Hook & Logic (`useVisualIntakeForm` gains the optional field).
  - [x] 6.3.4: View & Route (intake form gets a "Must be finished by" input; `RequestCard` shows it to the artist when present; e2e spec).

### 📦 Phase 7: Financial Engine, Payments & Policy Enforcement
- [x] **7.1: Deposit Payment Gateway Integration** (Integrate Stripe PaymentIntents/Checkout to collect required deposits upon request approval or slot lock-in. Scoped to client deposits only -- artist subscription billing, floated as a maybe in the original idea, is deferred to its own future roadmap item rather than bundled in here. Deposit collection stays decoupled from `RequestStatus`: approval/slot-lock works exactly as it does today, and once a request is `APPROVED`, the client dashboard offers a "Pay Deposit" step if their tier has a configured amount and `depositPaid` is still false -- no new status value, no change to the existing approve/confirm state machine. Artists set their own deposit amount per `ComplexityTier` rather than a platform-wide fixed percentage. Uses embedded Stripe Elements/PaymentIntents so the payment form stays in-app and on-brand, rather than a redirect to Stripe's hosted Checkout), decomposed per the Mandatory Task Breakdown Rule:
  - [x] 7.1.1: Data Gateway (`ArtistDepositSetting` -- per-artist, per-tier `depositAmount`, unique per artist+tier, no row for a tier means no deposit required yet, same default-open precedent as `ArtistWeeklyHours`; `IntakeRequest.depositAmount`/`stripePaymentIntentId`, a snapshot taken when the PaymentIntent is created so a later settings change never retroactively alters an in-flight or already-paid request; migration only, no application code).
  - [x] 7.1.2: Domain Service -- Deposit Settings (new `billing` domain, scaffolded with the mandatory `constants.ts`/`types.ts`/`billing.schema.ts` split; `getArtistDepositSettings`/`setArtistDepositSettings` upsert-per-tier; `ComplexityTier`/`COMPLEXITY_TIERS` reused from `intake` rather than redeclared; unit tests).
  - [x] 7.1.3: Domain Service -- Create Payment Intent (`createDepositPaymentIntent(intakeRequestId, clientProfileId)` in `billing`, ownership-checked against the trusted session's `clientProfileId` same as intake's `cancelIntakeRequest`; requires status `APPROVED`, `depositPaid: false`, and a configured tier amount; creates a Stripe PaymentIntent for that amount via a new `src/lib/stripe.ts` client singleton, persists `stripePaymentIntentId`/`depositAmount`; unit tests mocking the Stripe SDK).
  - [x] 7.1.4: Domain Service -- Confirm Payment (`confirmDepositPayment(paymentIntentId)` in `billing`, idempotent, called from the webhook handler; flips `depositPaid: true` on success; unit tests covering idempotency and mismatched/failed intents).
  - [x] 7.1.5: Controller/Action (`createDepositPaymentIntentAction`, `setArtistDepositSettingsAction`, `getArtistDepositSettingsAction`, and the Stripe webhook route `src/app/api/webhooks/stripe/route.ts` -- signature-verified via `STRIPE_WEBHOOK_SECRET`, delegates to 7.1.4; thin wrappers only).
  - [x] 7.1.6: UI Primitive & Config (`@stripe/stripe-js` + `@stripe/react-stripe-js` install, publishable-key config, a shared Elements provider wrapper; zero business logic).
  - [x] 7.1.7: Domain Hook & Logic (`useDepositPayment`/`useConfirmDepositPayment` -- client-side Elements/card-confirmation state, split into two hooks since `useStripe()`/`useElements()` only work inside an Elements tree; `useArtistDepositSettings` -- artist settings form state, mirroring `useArtistScheduleSettings`).
  - [x] 7.1.8: Domain Service -- Payable Deposits (`intake` gains a new, narrow read -- `getApprovedUnpaidRequestSummaries(clientProfileId)` -- exposing only `{id, artistId, tier}` for a cross-domain caller, without touching the existing `getClientBookings`/`ClientBookingSummary`; `getPayableDeposits` in `billing` is a pure function taking that array as input and joining it against `ArtistDepositSetting`, so billing never queries `IntakeRequest` directly and neither domain knows the other's table structure. The client dashboard page (7.1.9) composes both calls itself, same precedent as other server components calling multiple domain reads directly. Unit tests on both sides. Corrected mid-review from an earlier version where `getPayableDeposits` queried `IntakeRequest` directly -- matched an existing pattern elsewhere in the codebase, but that pattern is itself boundary-crossing debt, not one to extend).
  - [x] 7.1.9: View & Route ("Pay Deposit" card on the client dashboard for an eligible request, using 7.1.8's read; a "Deposit Amounts" section on the artist settings page; e2e spec, noting which parts need the user's own manual click-through in Stripe test mode).

- [x] **7.2: Cross-Domain Boundary Corrections** (Fixes real violations of the new Domain Boundary Isolation rule above, surfaced while reviewing 7.1.8: `billing`'s `createDepositPaymentIntent`/`confirmDepositPayment` query and mutate `IntakeRequest` directly via raw `prisma`/`tx` calls (4 call sites across those 2 files), and -- an older, pre-existing instance of the same problem, unrelated to Stripe -- `intake`'s `cancelIntakeRequest`/`cancelApprovedBookingAsArtist`/`rescheduleApprovedBooking` all reach directly into `scheduling`'s `TimeSlot` table via `tx.timeSlot.updateMany` to release a booking's slots, since scheduling has only ever exposed a slot-*creation* function (`createBookedTimeSlots`), never a release one. Sequenced ahead of 7.3 (old 7.2) rather than after, since Deposit Forfeiture & Refund logic would otherwise read/write the same `IntakeRequest` deposit fields from `billing` a third time, compounding the exact debt this item removes), decomposed per the Mandatory Task Breakdown Rule:
  - [x] 7.2.1: Domain Service -- Scheduling Slot Release (`releaseBookedTimeSlots(tx, intakeRequestId)` exported from `scheduling`, mirroring `createBookedTimeSlots`'s transaction-composable shape; unit tests. Purely additive -- existing callers are untouched until 7.2.2).
  - [x] 7.2.2: Domain Service -- Intake Call-Site Migration (`cancelIntakeRequest`/`cancelApprovedBookingAsArtist`/`rescheduleApprovedBooking` switch their raw `tx.timeSlot.updateMany` calls to 7.2.1's `releaseBookedTimeSlots`; existing unit tests continue to cover the same behavior, no test rewrites expected since the external contract doesn't change).
  - [x] 7.2.3: Domain Service -- Intake-Owned Deposit Read/Write (`intake` exposes narrow functions for `billing` to call instead of raw Prisma: a read by `intakeRequestId` and a read by `stripePaymentIntentId` -- both returning only the fields `billing` actually needs, not `intake`'s full row shape -- plus `recordDepositPaymentIntent`/`markDepositPaid` writes; unit tests).
  - [x] 7.2.4: Domain Service -- Billing Call-Site Migration (`createDepositPaymentIntent`/`confirmDepositPayment` switch to 7.2.3's functions instead of `prisma.intakeRequest` directly; existing unit tests continue to cover the same behavior, mocking intake's functions in place of the database rows they previously queried directly).

- [x] **7.3: Deposit Forfeiture & Refund Rules Engine** (Build business logic for automatic deposit retention vs. refund calculations based on cancellation timing and policies. The policy collapses to a flat lookup rather than a real sliding scale: self-cancel (5.4) is only ever reachable outside the existing 48-hour cancellation window, so it always refunds in full; an artist-initiated cancel (5.6) is never the client's fault, so it also always refunds in full regardless of timing; a no-show forfeits the deposit entirely, simply by never receiving a refund call -- no active "forfeited" state to track. Refund execution is automatic, firing as part of the same cancellation action rather than needing a separate artist-confirmed step), decomposed per the Mandatory Task Breakdown Rule:
  - [x] 7.3.1: Data Gateway (`BookingRequest.depositRefunded`/`stripeRefundId`, mirroring the `depositPaid`/`stripePaymentIntentId` precedent from 7.1.1; migration only).
  - [x] 7.3.2: Domain Service -- Booking-Owned Deposit Refund Read/Write (extends `BookingRequestDepositView` with `stripePaymentIntentId`/`depositRefunded`, updates both existing narrow reads to select them; adds `recordDepositRefund(bookingRequestId, stripeRefundId)`, mirroring `recordDepositPaymentIntent`/`markDepositPaid`; unit tests).
  - [x] 7.3.3: Domain Service -- Billing Refund Execution (`refundDeposit(bookingRequestId)` in `billing`, idempotent, calls Stripe's Refunds API then `recordDepositRefund`; unit tests mocking the Stripe SDK. Fixed mid-review before merge: the Stripe call and the local write are now in separate `try`/`catch` blocks -- a write-only failure logs and still reports success, since the refund already happened on Stripe's side and reporting failure would risk a caller retrying into a duplicate-refund attempt. Adds `confirmDepositRefund`, the refund-side mirror of `confirmDepositPayment`, invoked from a new `refund.updated` webhook case (`status === "succeeded"`) as the retry-safe reconciliation backstop for that failure mode -- the same role `payment_intent.succeeded` plays for `confirmDepositPayment`. Also prompted a new "External Side-Effect Safety" rule in the Architecture section above, codifying this pattern for any future domain service calling a third-party API with a real-world side effect).
  - [x] 7.3.4: Domain Service -- Wire Into Cancellation Flows (`cancelBookingRequest` (client) and `cancelApprovedBookingAsArtist` (artist) in `booking` each call billing's `refundDeposit` when the cancelled request had a paid deposit -- `booking` calling into `billing`'s exported service function, same cross-domain pattern already used for `scheduling`'s `createBookedTimeSlots`/`releaseBookedTimeSlots`, not a Domain Boundary Isolation violation. `markAppointmentNoShow` needs no change -- forfeiture is just the absence of a refund call. Unit tests covering both cancel paths).
  - [x] 7.3.5: View & Route (surface refund/forfeiture status on the client's booking card, e.g. "Deposit refunded"/"Deposit forfeited" -- requires threading `depositRefunded` through `getClientBookings`/`ClientBookingSummary`; e2e spec).

- [x] **7.4: Upfront Cancellation Precharge Engine** (Build a check referencing `ClientProfile.enforcePrecharge` to force a 50% upfront deposit route for flagged clients. Deposit-amount-only -- the artist's ability to approve a flagged client's request is unaffected ("No Auto-Booking" stays intact); only the deposit amount computed at payment time changes, reusing the existing approve -> pay-deposit flow with no new gate. The 50% precharge applies even when the artist hasn't configured any normal deposit for that tier at all -- a flagged client is exactly the case where a deposit should be required even if the artist normally wouldn't bother. When both a configured tier deposit and the precharge apply, the required amount is the greater of the two, so precharge can only ever raise the amount, never accidentally undercut what the artist would have asked for anyway), decomposed per the Mandatory Task Breakdown Rule:
  - [x] 7.4.1: Domain Service -- Booking-Owned Precharge Fields (extends `BookingRequestDepositView` with `estimatedPrice`/`clientEnforcePrecharge` -- `ClientProfile.enforcePrecharge` joined in via the existing `client` relation, so billing never queries `ClientProfile` directly -- updates both existing narrow reads to select/join them; unit tests. Purely additive).
  - [x] 7.4.2: Domain Service -- Billing Precharge Calculation (`createDepositPaymentIntent` computes the deposit as `max(configured tier amount ?? 0, clientEnforcePrecharge ? 50% of estimatedPrice : 0)` -- a new `PRECHARGE_PERCENTAGE` constant; "not configured" now only errors when both are unavailable; unit tests covering flagged-with-no-configured-deposit, flagged-with-a-smaller-configured-deposit, flagged-with-a-larger-configured-deposit, and unflagged-unaffected).

- [ ] **7.5: Day-of Bill Modifiers & Final Checkout** (A new dedicated flow -- not an extension of `AppointmentCard`/`AppointmentActions` -- reached via a "Checkout" link on the artist's past-due "Needs Resolution" section (`getPastDueAppointments`, 5.6.5) only, since checkout only makes sense once the appointment time has actually passed. The final bill credits the deposit already paid (if any) against the total: `estimatedPrice + Σaddons - (depositPaid ? depositAmount : 0)`. Finalizing checkout both saves the addons and transitions the booking to `COMPLETED`, reusing 5.6.2's `markAppointmentCompleted` -- checkout becomes the real mechanism for closing out an appointment, replacing the bare "Mark Completed" button for a booking that's reached checkout. Owned by `billing` (the blueprint's "Domain 3: Dynamic tabs, cancellation penalty engines"), calling into `booking`'s `markAppointmentCompleted` to finalize -- a `billing`-calls-into-`booking` cross-domain call, the reverse direction of 7.3.4's `booking`-calls-into-`billing` `refundDeposit`, both allowed under Domain Boundary Isolation since each calls the other's exported service function rather than reaching into its table directly), decomposed per the Mandatory Task Breakdown Rule:
  - [x] 7.5.1: Data Gateway -- a no-op. The `Addon` model (`label`, `price`, `bookingRequestId`) was already scaffolded into the schema in the very first migration and had simply gone unused since; no new migration needed.
  - [x] 7.5.2: Domain Service -- Booking-Owned Checkout Read (`getBookingRequestForCheckout(bookingRequestId)` in `booking`, exposing `{id, artistId, status, estimatedPrice, depositAmount, depositPaid}` -- exactly what billing's checkout flow needs, mirroring `getBookingRequestForDeposit`'s (7.2.3) precedent; unit tests).
  - [x] 7.5.3: Domain Service -- Addon Management (`addBillingAddon`/`removeBillingAddon`/`getBillingAddons` in `billing`, ownership-checked against the artist's session `artistId` via 7.5.2's read; unit tests).
  - [x] 7.5.4: Domain Service -- Final Bill & Finalize (`getFinalBillTotal(bookingRequestId)` computing the credited total above; `finalizeCheckout(bookingRequestId, artistId)` validating ownership/status then calling `booking`'s `markAppointmentCompleted`; unit tests).
  - [x] 7.5.5: Controller/Action (`addBillingAddonAction`, `removeBillingAddonAction`, `finalizeCheckoutAction` -- thin wrappers. The final-bill read is composed directly in the page server component instead, same precedent as `getPayableDeposits`/7.1.9's page composition).
  - [x] 7.5.6: Domain Hook & Logic (`useCheckout` orchestrating addon add/remove state and the finalize action's pending/error state -- each of the three actions gets its own `isPending`/`error` pair per the Independent Mutation State Isolation rule above, plus `removingAddonId` tracking which addon a remove is in flight for).
  - [x] 7.5.7: View & Route (new dedicated route, e.g. `/artist/[artistId]/appointments/[bookingRequestId]/checkout` -- booking summary, addon entry, running total showing the deposit credit, "Finalize & Complete" button; a "Checkout" link added to the past-due "Needs Resolution" section; e2e spec).

### 📦 Phase 8: Domain Rename -- Intake to Booking
> Project preference: "Booking" reads better than "Intake" for the domain/model that owns a client's request through its full lifecycle (submission through approval, decline, cancellation, completion). Full-depth rename covering the Prisma model, the domain folder, every hand-written identifier, and the database itself -- not just a code-level alias -- so the codebase and the database agree on the name going forward. Landed as one comprehensive change rather than layer-split PRs: renaming the Prisma model cascades instantly (`prisma.intakeRequest` breaks the moment the schema changes), so splitting this the normal way would leave the build broken between merges, the same class of exception as 4.2.1/4.4/6.2's bundling, just at full-codebase scale. Historical roadmap entries in Phases 1-7 above keep their original "intake"/"IntakeRequest" wording -- a record of what was actually built and named at the time, not a live spec; this Directory Blueprint and every phase from here on use the new name.
- [x] **8.1: Rename Intake to Booking** (`IntakeRequest` Prisma model -> `BookingRequest` (table, relation fields, FK columns, constraint names) via a `RENAME TO`/`RENAME COLUMN`/`RENAME CONSTRAINT` migration -- not DROP + CREATE -- so existing rows and their history carry forward untouched, same reasoning as the `RequestStatus` `RENAME VALUE` migration (5.6.1). `src/domains/intake/` -> `src/domains/booking/`, `intake.schema.ts` -> `booking.schema.ts`, and every hand-written identifier containing "Intake" renamed to "Booking" (`ClientIntakeInput` -> `ClientBookingInput`, `submitIntakeRequest` -> `submitBookingRequest`, `VisualIntakeForm`/`useVisualIntakeForm` -> `VisualBookingForm`/`useVisualBookingForm`, `IntakeRequestDepositView` -> `BookingRequestDepositView`, etc. -- Prisma-generated types weren't hand-edited, just regenerated). e2e fixtures (`global-setup.ts`/`global-teardown.ts`) and the dev seed script (`prisma/seed.ts`) updated to match the renamed table/columns).

> **Phases 9 onward were reordered in one pass** after Phase 7 shipped, folding every former Backlog item into the numbered roadmap and re-sequencing all of it (not just the promoted items) by criticality/value: real defects and safety gaps in already-shipped features first, then infrastructure with compounding payoff (CI coverage), then major feature work respecting the dependencies that already existed in the old text (the wizard explicitly wanted the confirm-dialog primitive first), then polish, then zero-user-impact internal debt, then the most speculative/largest-lift net-new features last. A second pass added four more gaps surfaced directly by the user (booking-process comprehension, design reference image management, appointment detail visibility, Needs Resolution's page layout) and re-sequenced the same way. Phase numbers below are this order, not the order these were originally scoped in.

### 📦 Phase 9: Safety Confirmations
> Promoted ahead of every other Phase 9+ item: an accidental destructive click (cancel, decline, mark no-show, etc.) has real consequences across both portals today, with nothing in front of it -- and Phase 17 (the booking wizard) already wants this primitive to exist first rather than build a throwaway one-off of its own.
- [x] **9.1: Action Confirmation Dialogs** (A shared confirm-dialog primitive gets wired in front of every destructive or state-changing action across both portals: booking request submission, cancellation, amendment, and status changes -- approve/decline/mark completed/mark no-show. No Data Gateway/Domain Service/Controller-Action layers needed -- every underlying action already exists, this only gates each behind a confirmation step), decomposed per the Mandatory Task Breakdown Rule:
  - [x] 9.1.1: UI Primitive & Config (a shared `ConfirmDialog` component, built on the same Radix/base-ui primitive family as the rest of `src/components/ui/`; zero business logic).
  - [x] 9.1.2: Domain Hook & Logic (a generic `useConfirmAction` hook wrapping "open dialog -> user confirms -> run the callback" state, reusable by any existing action regardless of domain; pure state orchestration, no new business rules).
  - [x] 9.1.3: View & Route -- Client Portal (wires 9.1.1/9.1.2 into booking request submission, cancel, and amend: `VisualBookingForm`, `useClientBookingActions`'s cancel/save-edit buttons; e2e spec).
  - [x] 9.1.4: View & Route -- Artist Portal (wires 9.1.1/9.1.2 into approve/decline, reschedule, cancel-as-artist, mark no-show, and checkout's finalize: `RequestActions`, `AppointmentActions`, `AppointmentLifecycleActions`, `CheckoutView`; e2e spec).

### 📦 Phase 10: Client Profile Management
> Closes a gap surfaced while building 6.2: `firstName`/`lastName`/`dateOfBirth` lock permanently once set, with the booking form as the only place they're ever collected -- a client with a typo, or who just wants to update their phone number, currently has no way to ever correct it. Session-derived like the client dashboard (`/client`, no `clientProfileId` URL param), mirroring Phase 20's analogous artist-facing profile editing.
- [x] **10.1: Client Profile Page** (A client-facing page for viewing/editing their own `ClientProfile` details -- name, email, phone, date of birth, Instagram handle -- reachable from the client dashboard. The read side reuses `getClientProfileContactDetails` (6.1.1) as-is -- it already returns every field needed -- so this is purely additive: a write plus its own layers), decomposed per the Mandatory Task Breakdown Rule:
  - [x] 10.1.1: Domain Service (`updateClientProfile(clientProfileId, fields)` in `booking`, alongside `ClientProfile`'s existing read/write services; handles `instagramHandle`'s unique-constraint conflict gracefully as a friendly error rather than a raw Prisma exception; unit tests).
  - [x] 10.1.2: Controller/Action (`updateClientProfileAction`, deriving `clientProfileId` from the trusted session; a new `updateClientProfileInputSchema` in `booking.schema.ts`).
  - [x] 10.1.3: Domain Hook & Logic (`useClientProfile` orchestrating fetch/edit/save state).
  - [x] 10.1.4: View & Route (the profile page itself, editable fields, a link from the `/client` dashboard; e2e spec).

### 📦 Phase 11: Client Cancellation History Visibility for Artists
> Surfaced while scoping 7.4 -- the artist currently has zero visibility into a client's cancellation/no-show history (`ClientProfile.cancellationCount`/`enforcePrecharge`) when reviewing a request on `RequestCard`, even though 7.4's precharge engine already reacts to that same flag on the billing side. Promoted from the Backlog: this closes the loop on an already-shipped feature's real-world usefulness rather than adding something new.
- [x] **11.1: Cancellation History Flag on RequestCard** (A visible flag/badge on `RequestCard` surfacing a client's `cancellationCount`/`enforcePrecharge` state, so the artist can factor it into their own approve/decline decision, not just the deposit amount 7.4 already computes from it. No Controller/Action layer -- `getPendingBookingRequests` is already called directly from the artist dashboard page, same precedent as elsewhere), decomposed per the Mandatory Task Breakdown Rule:
  - [x] 11.1.1: Domain Service (extend `PendingBookingRequestSummary` with `clientCancellationCount`/`clientEnforcePrecharge`; update `getPendingBookingRequests` to map them from the already-included `client` relation -- purely additive, no new query; unit tests).
  - [x] 11.1.2: View & Route (`RequestCard` shows a visible flag/badge when `clientCancellationCount > 0` or `clientEnforcePrecharge` is true; e2e spec).

### 📦 Phase 12: Booking Process Explainer
> Surfaced directly by the user: a first-time client has no way to know, before submitting, that the flow is request -> artist review -> (if approved) pay a deposit to actually lock in the slot -- the request form and the client dashboard both just present each stage in isolation with no framing of the sequence. Ranked alongside the other real-gap items above rather than with pure polish, since it affects every guest's first impression of the whole booking flow.
- [x] **12.1: "How Booking Works" Explainer** (Static copy on `/book/[artistId]` -- and Phase 17's wizard Step 4 summary once that ships -- laying out the request -> review -> deposit -> locked-in sequence explicitly, so a client isn't surprised that approval doesn't itself confirm the slot), decomposed per the Mandatory Task Breakdown Rule:
  - [x] 12.1.1: View & Route (a small, reusable `BookingProcessExplainer` component -- static copy, no props/logic -- composed into `/book/[artistId]/page.tsx` above the form; built as its own component rather than inlined since Phase 17's wizard Step 4 summary is explicitly meant to reuse it).

### 📦 Phase 13: Design Reference Image Management
> Surfaced directly by the user. The booking form's current image UX (`VisualBookingForm.tsx`) is upload-only -- `DesignReferenceDropzone` appends to `designReferenceImageUrls` with no remove control and only a bare 96x96 thumbnail, no larger preview. Ranked high, alongside the other core-flow gaps above: reference images aren't a minor form field here, they're one of the Project Blueprint's two mandatory intake requirements ("Visual Enforcement & Filtering") -- getting this UX wrong directly undermines the thing the whole app is built around. Sequenced before Phase 17 (the wizard) so the wizard's Step 2 inherits the improved UX instead of needing its own separate pass.
- [x] **13.1: Add/Remove/Preview on the Booking Form** (Before first submission: a remove control per uploaded thumbnail, and a larger preview (e.g. a click-to-enlarge dialog) instead of the bare 96x96 image -- re-addable after removal, same as before any upload), decomposed per the Mandatory Task Breakdown Rule:
  - [x] 13.1.1: UI Primitive & Config (a plain `Dialog` component, `src/components/ui/dialog.tsx` -- same wrapping pattern as `alert-dialog.tsx` but for a passive, dismiss-only preview rather than a confirm/cancel decision; zero business logic).
  - [x] 13.1.2: Domain Hook & Logic (`useVisualBookingForm` gains a `removeDesignReferenceImage(url)` function filtering `designReferenceImageUrls`; re-addable afterward since the existing dropzone/upload flow is untouched. Bundled a real bug fix found while touching this same field: `handleUploadComplete` was replacing the whole array with just the newly-uploaded batch instead of appending, silently wiping out any earlier add).
  - [x] 13.1.3: View & Route (a remove control on each uploaded thumbnail and a click-to-enlarge preview, using 13.1.1's `Dialog`, wired into `VisualBookingForm.tsx`; e2e spec).
- [x] **13.2: Extend PENDING-Request Editing to Images** (5.4's self-service edit of a still-PENDING request explicitly excludes `tier`/`tags`/images today, re-running the full validation pipeline being judged not worth it for notes/budget/date alone at the time. Lifts that exclusion for images specifically, reusing 13.1's add/remove/preview UI, so a client can fix a wrong or low-quality reference image without cancelling and resubmitting the whole request. Also closes a read-side gap found while scoping this: `ClientBookingSummary` doesn't expose a booking's existing `designReferenceImageUrls` at all today, so the edit form has nothing to show/remove from without first extending the read), decomposed per the Mandatory Task Breakdown Rule:
  - [x] 13.2.1: Domain Service -- Read (`ClientBookingSummary` gains `designReferenceImageUrls`; `getClientBookings` updated to select/map the request's `DesignReference` rows; unit tests).
  - [x] 13.2.2-13.2.3: Domain Service -- Write + Controller/Action, shipped as a single bundled PR (same coupling as 4.2.1/4.4/6.2: making `designReferenceImageUrls` required on `UpdatePendingBookingRequestInput` breaks `updatePendingBookingRequestAction`'s `...parsed.data` spread the moment the type changes, until `updatePendingBookingRequestInputSchema` also supplies the field -- splitting it the normal way would leave the build broken between merges):
    - Domain Service: `UpdatePendingBookingRequestInput`/`updatePendingBookingRequest` gain `designReferenceImageUrls`, replacing the request's `DesignReference` rows transactionally, validating at least one image remains; unit tests.
    - Controller/Action: `updatePendingBookingRequestInputSchema` gains the images field, reusing the existing `designReferenceImagesSchema`.
  - [x] 13.2.4: Domain Hook & Logic (`useClientBookingActions`'s edit state gains image add/remove management, mirroring `useVisualBookingForm`'s (13.1.2) append/remove pattern).
  - [x] 13.2.5: View & Route (`ClientBookingActions.tsx`'s edit form gains the dropzone + remove/preview UI, reusing 13.1.1's `Dialog`; e2e spec -- covers the full add/remove/preview interaction against seeded existing images, closing the gap 13.1.3's spec had to defer).

### 📦 Phase 14: CI Pipeline Maturity
> The two follow-ups explicitly staged separately when the CI pipeline (`.github/workflows/ci.yml`) was first built (see Automated Testing Guardrails above), promoted from the Backlog. High compounding value -- the earlier these land, the more of the rest of this roadmap ships with real coverage behind it.
- [x] **14.1: Stripe Test-Mode Integration Tests in CI** (A second CI tier alongside the mocked unit suite, making real calls against Stripe's test mode (no mocking) -- needs a `STRIPE_SECRET_KEY` GitHub Actions secret), decomposed per the Mandatory Task Breakdown Rule:
  - [x] 14.1.1: Test Harness & Config (a separate vitest project/script -- `vitest.stripe.config.mts` + `npm run test:stripe` -- picking up a new `*.stripe-integration.test.ts` pattern, kept out of the default `npm run test` run so local/CI unit runs never need a real Stripe key; zero business logic, just plumbing).
  - [x] 14.1.2: Integration Tests -- Payment Intent Lifecycle (`createDepositPaymentIntent` + `confirmDepositPayment` against a real Stripe test-mode PaymentIntent, confirmed via Stripe's test payment-method token to reach `succeeded`. Surfaced and fixed a real bug the mocked unit suite couldn't see: `createDepositPaymentIntent`'s Stripe call had no `automatic_payment_methods` config, so Stripe defaulted to Dashboard-enabled payment methods including redirect-based ones, which require a `return_url` this app's embedded, in-app Elements flow never provides -- confirmation failed outright until `automatic_payment_methods: { enabled: true, allow_redirects: "never" }` was added).
  - [x] 14.1.3: Integration Tests -- Refund Lifecycle (`refundDeposit` + `confirmDepositRefund` against a real refund issued on the PaymentIntent from 14.1.2's pattern).
  - [x] 14.1.4: Integration Tests -- Webhook Signature Verification (the actual route handler's `stripe.webhooks.constructEvent` call exercised with a real Stripe-signed payload via `generateTestHeaderString`, covering valid and invalid signatures -- not mocked. The route handler had no test at any tier before this -- a mocked Stripe client can't meaningfully exercise its own signature check. Event-type dispatch logic beyond `payment_intent.succeeded` (`refund.updated`, `payment_intent.payment_failed`) is a separate concern from signature verification and stayed out of scope, noted as a possible future gap).
  - [x] 14.1.5: CI Wiring (a new job/step in `ci.yml` running `npm run test:stripe`, gated on the `STRIPE_SECRET_KEY` secret being present so a fork/PR without it doesn't fail. Landed ahead of 14.1.3/14.1.4 rather than after -- the user asked whether the pipeline already ran these tests before those two existed, and wiring `npm run test:stripe` in now needs no further CI changes once they do land, since it already picks up any file matching the naming pattern).
- [x] **14.2: E2E Tests in CI** (Run the existing Playwright suite (`e2e/`) in the pipeline itself, not just as the local manual verification gate -- needs a Postgres service container plus a Playwright browser install in the runner; `e2e/global-setup.ts`/`global-teardown.ts`'s fixture flow already does most of the hard work, just needs wiring into a job. Also needed real `STRIPE_SECRET_KEY`/`STRIPE_PUBLISHABLE_KEY` repo secrets, not the unit job's dummy placeholders -- `deposit-payment.spec.ts` drives a real PaymentIntent creation and a real Stripe Elements mount).

### 📦 Phase 15: Stripe Idempotency Safeguards
> Promoted from the Backlog. `createDepositPaymentIntent`/`refundDeposit` call Stripe's API without an explicit idempotency key, so a client-side retry after a dropped response (not a failed call -- the call itself already has try/catch and a webhook reconciliation backstop, per External Side-Effect Safety) could in principle create a second PaymentIntent/refund for the same booking. Lower urgency than Phase 9-14 -- the webhook backstop already limits the blast radius, and a UI-level double-submit is already guarded by each mutation hook's own pending state -- but it addresses duplicate-request prevention specifically, which External Side-Effect Safety's existing coverage doesn't.
- [x] **15.1: Idempotency Keys on Deposit/Refund Calls** (Pass a stable idempotency key -- derived from the `BookingRequest` id and an action discriminator -- to `stripe.paymentIntents.create`/`stripe.refunds.create`, so a retried call against an already-in-flight or already-completed request returns the original result instead of creating a duplicate. Single Domain Service sub-task -- both functions already existed, this only added the `idempotencyKey` request option to their existing Stripe calls, no new layers needed):
  - [x] 15.1.1: Domain Service (`DEPOSIT_PAYMENT_INTENT_IDEMPOTENCY_KEY_PREFIX`/`DEPOSIT_REFUND_IDEMPOTENCY_KEY_PREFIX` constants; `createDepositPaymentIntent`/`refundDeposit` pass `{ idempotencyKey: \`${prefix}:${bookingRequestId}\` }` as the Stripe SDK's second request-options argument; existing unit tests in `billing` and the two `booking` cancellation-path tests that mock the same refund call updated to assert the new argument).

### 📦 Phase 16: Client Profile Email Uniqueness
> Surfaced directly by the user while reviewing 10.1.1's `isInstagramHandleConflict` check: `ClientProfile.email` has no unique constraint today (only `instagramHandle` does), a deliberate, already-documented gap from Phase 5.2 ("a guest booking made under a different email than the one on file won't auto-link... since `ClientProfile.email` itself isn't unique"). Not bundled into 10.1 -- this is a business-logic change (redesigning how a guest booking matches/creates a `ClientProfile`), not a profile-page concern. `submitBookingRequest`'s guest-upsert currently keys purely on `instagramHandle`, so one person can legitimately end up with two rows sharing an email today (e.g. booking under a personal and a business Instagram handle) -- making email unique without addressing that would turn a silently-accepted case into a hard failure, and the migration itself would fail outright if the DB already has duplicate emails. Moderate value (real data-integrity gap, unblocks cleaner guest-to-signup linking), not urgent -- sequenced with the other correctness-hardening items rather than the top-priority cluster.
- [x] **16.1: Make ClientProfile.email Unique** (Redesign `submitBookingRequest`'s guest-upsert matching to handle an email that already exists under a different `instagramHandle` -- merge into the existing profile rather than erroring, or a clear conflict message, whichever the matching logic ends up needing -- then add the `@unique` constraint + migration, with any existing duplicate emails resolved first), decomposed per the Mandatory Task Breakdown Rule:
  - [x] 16.1.1: Data Gateway (migration with raw SQL to deduplicate existing `ClientProfile` rows sharing an email -- per duplicate group, keep the row an `Account` is already linked to, else the earliest-created row; reassign `BookingRequest.clientId`/`Account.clientProfileId` off the other rows onto the kept row; delete the rest -- then add `email String @unique` to the Prisma schema. No application code.).
  - [x] 16.1.2: Domain Service (extract the guest-profile-resolution logic out of `submitBookingRequest` into a new, unit-testable `resolveGuestClientProfile` service in `booking/services/`. New matching order: match by `instagramHandle` first, unchanged from today; if no handle match, fall back to matching by `email` and reuse that profile rather than erroring -- filling only currently-null fields, never overwriting, the same "fill blanks only" posture 6.1.3/6.2 already use for a signed-in client. `submitBookingRequest` calls this instead of its inline upsert; unit tests covering new-handle+new-email create, existing-handle update, and new-handle-but-existing-email reuse-and-fill-blanks).

### 📦 Phase 17: Client Booking Wizard (Multi-Step Booking Form)
> Sequenced after Phase 9 (confirm-dialog dependency) and Phase 13 (inherits the improved image UX) on purpose -- see their notes.
- [x] **17.1: Multi-Step Client Booking Form** (Restructure `VisualBookingForm` into a four-step wizard with a progress bar, replacing the current single-scroll layout. Purely a reorganization of already-existing, already-validated fields into steps plus new wizard/step state -- no schema or domain-service changes. Step 1 (Contact Details): First Name, Last Name, Date of Birth, Email, Phone, Instagram Handle -- skipped entirely for a signed-in client with a complete profile, who instead lands on Step 2 behind a compact "Booking as {name} ({email}) · Not you? Log out" banner, preserving the per-field lock/prefill behavior from 6.1.3/6.2 for any still-missing field. Step 2 (Service, Budget & Canvas): Service Tier, Tags, Notes, Budget Range, Reference Images (Phase 13's improved add/remove/preview UX). Step 3 (Date & Slot Selection): date + fixed daily time slot filtered strictly by existing operating-hours/booked-slot availability (4.1.10/4.3) -- no duration-based filtering, since duration stays an artist-only decision at approval time per the "No Auto-Booking" core constraint -- plus the optional Client Max End Time (6.3). Step 4 (Request Summary & Confirmation): read-only recap of every collected field, uploaded image thumbnails, Phase 12's "How Booking Works" explainer copy, and a "Submit Booking Request" CTA that submits immediately -- no confirm-dialog interstitial yet; Phase 9.1's shared confirm-dialog primitive attaches here once it ships, rather than this wizard building its own one-off that 9.1 would later have to replace. Progress-bar step navigation is completion-bounded: a client can always go back to a previously completed step, and forward only as far as steps that have already passed their own step-level Zod validation. Excludes Placement/Canvas Metadata and Reference Image Annotations -- both unscoped items with no domain service yet; their fields plug into Step 2 once those items are built out separately), decomposed per the Mandatory Task Breakdown Rule:
  - [x] 17.1.1: UI Primitive & Config (stepper/progress-bar primitive + sticky mobile bottom action bar, zero business logic).
  - [x] 17.1.2: Domain Hook & Logic (wizard step/progress state built around the existing `useVisualBookingForm`, per-step Zod validation gating "Next" -- including designReferenceImageUrls on Step 2, corrected mid-review after initially being left out over an e2e-testability concern that a mocked UploadThing network flow (`e2e/wizardHelpers.ts`) solved properly instead -- skip-step-1-if-logged-in-and-complete logic, completion-bounded step-jump logic, and a `goToFirstInvalidStep` recovery path for any final-submit validation failure the review step itself doesn't render).
  - [x] 17.1.3: View & Route (the four step views + summary step composed into `VisualBookingForm.tsx`, progress bar wiring, logged-in banner; e2e spec covering a guest's full-wizard submission (a real end-to-end success, "Request submitted.", via the mocked-upload flow above), a signed-in client skipping Step 1, backward navigation preserving entered state, and a blocked forward-jump past an invalid step. Two real bugs fixed mid-review from manual click-through: the logout banner nested a `<form>` inside the wizard's own outer `<form>`, invalid HTML Next.js only flagged as a console hydration error; and `global-teardown.ts` only deleted seeded fixture `BookingRequest` rows by id, leaving the new spec's genuinely-submitted request (and its guest `ClientProfile`) to orphan and break a later run's Artist cleanup -- extended to also catch requests by `artistId` regardless of who created them).

### 📦 Phase 18: Upcoming Appointment Detail Expansion ◄ CURRENT FOCUS
> Surfaced directly by the user: selecting an upcoming (or past-due) appointment should expand it -- inline or a side panel -- into the full booking detail, including uploaded reference images, rather than the flat `AppointmentCard` summary today. Folds in a related, smaller complaint: `AppointmentCard` already conditionally renders `estimatedPrice` when non-null (every `APPROVED` request has one via 4.4's enforcement at review time), so a booking showing no price today is most likely a null value from directly-seeded data rather than a rendering gap -- this phase's expanded detail view is the place to make it reliably prominent regardless. Sequenced right before Phase 19 (the calendar view), which already wants this same "click a booking, see a detail modal/drawer" interaction for its own list -- building it once here against the existing list lets Phase 19 reuse it instead of building its own.
- [ ] **18.1: Appointment Detail Panel** (Selecting an `AppointmentCard` on `/artist/[artistId]/appointments` expands a detail panel/drawer with the full booking -- every reference image at full size, estimated price always shown, tags, notes, client contact -- reusing 4.5/5.6.5's existing `getUpcomingAppointments`/`getPastDueAppointments` reads, no new domain service expected: confirmed during scoping that `UpcomingAppointmentSummary` (shared by both reads) already carries every field a detail view needs), decomposed per the Mandatory Task Breakdown Rule:
  - [ ] 18.1.1: UI Primitive & Config (a wider size variant on `src/components/ui/dialog.tsx`'s `DialogContent`, currently fixed `max-w-lg` -- a detail view with multiple full-size images and full client info needs more room than the existing image-preview dialogs from 13.1.1/13.2.5; zero business logic).
  - [ ] 18.1.2: View & Route (`AppointmentDetailDialog`, reusing the existing `Dialog`/`DialogTrigger` pattern, wired per-card so clicking an `AppointmentCard` opens it; full-size reference images, estimated price always rendered, all tags, notes, full client contact; no new domain hook needed -- pure JSX composition over data the existing reads already fetch; e2e spec).
- [ ] **18.2: Seed/Fixture Data Fixed to Match Real Invariants** (Scoped to the concrete gaps actually found while investigating 18's estimated-price question, not a claimed-exhaustive audit -- "every edge case" isn't provable complete, so this fixes the specific instances confirmed by inspection rather than promising more than that. `prisma/seed.ts`'s `APPROVED` fixtures never set `estimatedPrice`, even though the real approval path (`reviewBookingRequest`) makes it mandatory -- add it to every `APPROVED`-and-beyond fixture (`APPROVED`, and anything only reachable via a prior `APPROVED`: `COMPLETED`, `NO_SHOW`, a from-`APPROVED` `CANCELLED_BY_CLIENT`/`CANCELLED_BY_ARTIST`). Same class of gap on the money side: `prisma/seed.ts` sets `depositPaid: true` (its two paid fixtures) without ever setting `depositAmount`/`stripePaymentIntentId`, and `e2e/global-setup.ts` sets `depositRefunded: true` (its refunded-deposit fixture) without `stripeRefundId` -- the real `confirmDepositPayment`/`confirmDepositRefund` flows always set these together. Add the missing companion fields wherever the corresponding boolean is set to `true` in both files), scoped during investigation to a single sub-task -- fixture/seed code isn't application code, so it doesn't decompose into the usual architectural layers:
  - [ ] 18.2.1: Fixture Data Only (extend `upsertRequest()` in `prisma/seed.ts` to accept `estimatedPrice`/`depositAmount`/`stripePaymentIntentId`, threaded into the 4 APPROVED fixtures -- `vettrApprovedFuture`, `vettrApprovedPaid`, `vettrPastDue`, `lunaApprovedFuture` -- plus deposit companions on the 2 that set `depositPaid: true`; add `estimatedPrice` to the 8 APPROVED-or-beyond INSERTs in `e2e/global-setup.ts`, plus `depositAmount`/`stripePaymentIntentId`/`stripeRefundId` on the `refundedDepositRequestId` fixture. No production code touched).

### 📦 Phase 19: Artist Agenda & Calendar View
- [ ] **19.1: Day/Week Calendar Agenda** (A dedicated agenda page with a toggleable day/week calendar picker; selecting a date filters the booking list, and clicking a booking opens the same detail panel/drawer as Phase 18, with quick-action status controls added alongside it. Internal display only -- no external calendar sync in this phase).

### 📦 Phase 20: Artist Settings Restructure
- [ ] **20.1: Decouple the Combined Settings Page** (Split the current single `/artist/[artistId]/hours` page into dedicated sub-views: Profile Management -- folds in the former "Artist Profile Management UI" backlog item, editing `avatarUrl`/`bio`/`location` -- Business & Operating Hours, Time Slot Rules & Service Durations, and Blackout Dates & Multi-Date Range Overrides, the last supporting continuous date-range selection for extended breaks rather than single dates only).

### 📦 Phase 21: Needs Resolution as a Dedicated Page
> Surfaced directly by the user. `/artist/[artistId]/appointments` currently bundles the past-due "Needs Resolution" section (5.6.5) together with the upcoming-appointments list on one page; splitting it out mirrors the same "one combined page doing too much" reasoning as Phase 20's settings restructure.
- [ ] **21.1: Split Needs Resolution Out** (A dedicated `/artist/[artistId]/appointments/needs-resolution` (or similar) route for `getPastDueAppointments`, leaving `/artist/[artistId]/appointments` as the upcoming-only list; a nav link/badge between the two so an artist with pending resolutions doesn't lose visibility into them by not being on the combined page anymore).

### 📦 Phase 22: Universal Back Navigation
- [ ] **22.1: Universal Back Navigation** (Audit every client/artist sub-page and modal/overlay for a consistent back control, via a shared navigation component rather than one-off links).

### 📦 Phase 23: Budget Input Enhancements
- [ ] **23.1: Budget Slider & Wheel Picker Polish** (Freeze the numeric readout during drag to stop jitter, add a touch-friendly wheel-select alternative, and enforce strict £5 step increments on both controls).

### 📦 Phase 24: Layout & Responsive Consistency Polish
- [ ] **24.1: Request Page Padding Standardization** (`/book/[artistId]/page.tsx` currently has no wrapper/padding classes at all -- confirmed gap -- unlike `/artists`/`/client`'s shared `mx-auto w-full max-w-lg px-4 py-4` container pattern. Bring it in line).

- [ ] **24.2: Nested Viewport Unit Audit** (The root shell already uses dynamic viewport units -- `min-h-[100dvh]` on `<body>`, `viewportFit: "cover"` in the `Viewport` export -- so this is an audit of remaining nested views/components for any leftover fixed-height assumptions, not a rebuild of the shell).

### 📦 Phase 25: Hook State Hygiene Debt
> Promoted from the Backlog. Both zero-user-impact-today code quality gaps, not defects a user can currently hit -- sequenced near the bottom on that basis, ahead only of purely speculative net-new features.
- [ ] **25.1: Fix the `react-hooks/set-state-in-effect` Sites CI's Lint Step Surfaced** (The rule is downgraded to `warn` -- see Automated Testing Guardrails above -- rather than fixed, so these still need a real look: `src/components/ThemeToggle.tsx` (reads DOM state synchronously on mount), `src/domains/billing/components/DepositAmountsSettings.tsx` and `src/domains/scheduling/components/BusinessHoursSettings.tsx`'s `DayHoursRow` (both sync local edit state to a prop -- the latter already has a working fix, the render-time "adjust state when a prop changes" pattern instead of an effect, that was written and verified during the CI investigation but reverted to keep that PR in scope, so it can be reapplied directly), and the mount-fetch pattern in `useArtistDepositSettings`/`useArtistScheduleSettings` (harder: wrapping the effect's call site in an inline `async` IIFE satisfies the linter but doesn't fix anything real, since the code is otherwise identical -- likely wants React's `useEffectEvent` escape hatch instead, or a rethink of the fetch-on-mount pattern itself)).
- [ ] **25.2: Retrofit Independent Mutation State Isolation onto Pre-Existing Hooks** (The rule (Architecture section) only applies going forward from `useCheckout`'s (7.5.6) review; `useArtistScheduleSettings`'s `setDayHours`/`setOverride` share one `isPending`/`error` pair the same way `useCheckout` did before its fix, and are the known pre-existing instance of the same gap).

### 📦 Phase 26: Tier Reference Gallery Management UI
> Promoted from the Backlog.
- [ ] **26.1: Artist-Facing Gallery Management** (Upload/reorder/remove for the artist's own `TierReferenceImage` rows (4.6). Images are seeded via Prisma Studio in the meantime).

### 📦 Phase 27: Placement & Canvas Metadata
> Promoted from the Backlog.
- [ ] **27.1: Placement & Canvas Metadata Fields** (Capture body location / nail set context fields in booking schemas, e.g. "Left Forearm", "Full Set - Natural Nails" -- plugs into Phase 17's Step 2 once built).

### 📦 Phase 28: Reference Image Annotations
> Promoted from the Backlog.
- [ ] **28.1: Per-Image Annotations** (Allow clients to tag specific reference images with notes during booking, e.g. "Use color from Image 1, but shape from Image 2" -- plugs into Phase 17's Step 2 once built).

### 📦 Phase 29: Artist Custom Response Templates
> Promoted from the Backlog.
- [ ] **29.1: Quick-Copy Response Templates** (Text blocks or automated emails for approval, decline, or off-platform follow-up notifications).

### 📦 Phase 30: In-App Client/Artist Chat
> Promoted from the Backlog. Sequenced last -- the largest, most speculative lift of everything in this roadmap (its own messaging subsystem, notifications, etc.), and Instagram DM already works as the interim off-platform channel today.
- [ ] **30.1: In-Platform Messaging** (On-platform messaging tied to a specific `BookingRequest`, so review/follow-up conversation doesn't have to happen off-platform over Instagram DM. Part of why client auth (5.2) is password-based rather than magic-link -- chat needs frequent, low-friction re-entry. Likely still wants email notifications for new messages, possibly with a scoped link straight into the thread).

### 📦 Phase 31: Interactive Moodboard / Canvas Design Tool
> Surfaced directly by the user: an interactive, Canva-style moodboard tool for clients to dynamically craft or refine their design concepts -- a guided canvas framework, data-driven export, annotation and feedback notes, smart snapping. Sequenced last alongside Phase 30, on the same footing by the user's own judgment and Claude's -- a real canvas engine, a serialization/export data format, and annotation threading is a multi-phase epic in its own right, not a small addition. Likely supersedes/absorbs the smaller Reference Image Annotations (27) and Placement & Canvas Metadata (28) items rather than duplicating them, but that's a call for its own dedicated scoping pass, not decided here -- no sub-task breakdown yet.
- [ ] **31.1: Moodboard Tool** (Unscoped -- needs its own dedicated Mandatory Task Breakdown Rule pass, likely spanning a canvas UI primitive/library choice, a data model for canvas elements/annotations, an export pipeline, and real-time or save-on-change persistence, before any of this is broken into layers).

## 🌿 Git & Agent Workflow (Atomic Scope Strategy)
- **Branch Strategy:** Never execute major code generations or package installations directly on `main`.
- **Atomic Functional Scope:** Claude must treat **every numbered bullet point** (e.g., `2.1`, `2.2`) as a single, isolated, conceptually complete Pull Request. Do not combine or cross-pollinate different numbered tasks into a single run.
- **Numbering Scheme:** Sub-tasks are dotted numeric, not lettered (e.g. `4.1.1`, `4.1.2`, ... never `4.1a`, `4.1b`). Nest as deep as the work actually needs — a sub-task that itself needs further layer decomposition gains its own numeric children (e.g. `4.1.10` decomposing into `4.1.10.1`-`4.1.10.4`, a "quad"/four-part number), rather than switching to letters at the next level down.
- **Vertical Feature Completion:** A numbered roadmap item (e.g. `4.1`) is one complete feature, backend through frontend — never split into separate top-level numbers for "backend now" and "frontend later." Backend and frontend layers are sibling numbered sub-tasks under that same number (`4.1.1`, `4.1.2`, ...), per the Mandatory Task Breakdown Rule below, so a feature can never ship backend-only with its UI merely tracked for someday. If a feature's UI surface isn't fully known when its backend sub-tasks are first scoped, still reserve and list the trailing UI sub-task(s) as unchecked placeholders in that same pass, immediately after the backend ones.
- **Mandatory Task Breakdown Rule:** This is the standard decomposition pattern for every numbered roadmap item going forward, not just ones that look unusually large. Before writing code, Claude MUST propose numbered sub-tasks (`.1`, `.2`, `.3`, ...) for each architectural layer the feature touches — Data Gateway, Domain Service, Controller/Action, UI Primitive & Config, Domain Hook & Logic, View & Route, in that order where applicable — and get the user's go-ahead on the breakdown first. A single PR must NEVER cross layer boundaries (e.g., adding UI primitives AND writing complex hooks AND setting up page routes in one run).
- **Blast Radius Boundaries:** Data Gateway PRs (schema/migrations) must contain no application code, just Prisma models and/or raw SQL. Domain Service PRs must be pure business logic plus their unit tests, no Prisma calls beyond what the service itself needs. Controller/Action PRs must be thin `actions.ts` wrappers (validate, delegate, shape the result) with no business rules of their own. UI Primitive & Config PRs must be max setup files with zero business logic. Domain Hook & Logic PRs must focus purely on state management, validation, and domain services. View & Route PRs must focus purely on JSX composition and page routing.
- **Zero Context Bleed:** Absolute ban on adding unrelated "quick styling updates", formatting changes, or side-fixes to files outside the direct functional requirement of the active sub-task.
- **Review Interception:** Upon completing a single numbered sub-task, Claude must pause, execute `npm run test` to verify zero system regressions, present the target file diff map to the user, and request confirmation before starting the next item. For view/route-layer sub-tasks, this also means adding/updating the relevant `e2e/*.spec.ts` and explicitly telling the user to run `npm run test:e2e:ui` (see Manual E2E Verification Gate) as part of requesting their go-ahead.
- **Commit Standards:** Use clear, atomic git summaries matching the domain (e.g., `feat(booking): add zod schema validation for instagram handles`, `test(booking): implement logic checks for design complexity`).
- **Mandatory PR Description Policy** Never raise a PR without this description block. Include a brief summary of the change and why it was made. The `gh` CLI is installed and authenticated (`C:\Users\thang\tools\bin\gh.exe`, on the user PATH) — after pushing, Claude must raise the PR directly with `gh pr create --title "..." --body "..."`, passing the description block via `--body`, rather than handing the user a manual compare-URL link.